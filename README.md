BACKEND

//Password Hashing (with bcrypt) 🔒
//User Login + JWT Auth 🔐
//CORS + Frontend Connection (React) 🔄
//Form Validation - YUP - react/node
//middleware for yup
//middleware for protected routes - token + roles
//create utility for secretkey, random token generation.
//reset password/ forgot password api
 
/ change name of token to resetToken
/ search GET api , pagination , sorting, filtration / -- testing

/ add cron job , to empty error.log file, after 1 day.
/ add every type of input fields in form, like radio, checkbox, img, select.




------------------------------
FRONTEND

-- reset password
-- image upload
- after login , 'keep logged in'
- Logout / token expiry handling
- Validating the JWT token using expiry
- live search bar

//Protected routes (auth middleware/COMPONENT)
//Decode the token to get user info
// Handle logout
// Add a loading spinner for auth checks
// show round icon loading, during form submission, 
// show toaster for Success and on Fail.
// frontend form validation

